# 产品需求文档 (PRD) - Job Dashboard Local

## 1. 产品概述 (Product Overview)

### 1.1 产品定位
**Job Dashboard Local (本地求职看板系统)** 是一款为求职者量身定制的本地化职位管理与分析工具。它通过统一处理来自多招聘平台（Boss直聘、51job、智联招聘、猎聘等）的原始数据，为求职者提供统一的看板、全生命周期的投递追踪、以及 AI 驱动的职位匹配分析。

### 1.2 目标用户
- 需要海投且同时使用多个招聘平台的求职者。
- 难以管理大量投递进度、面试记录的用户。
- 期望借助 AI 评估自身简历与特定岗位匹配度，并生成专业沟通话术的用户。
- 高度关注个人求职数据隐私，倾向于本地存储管理的用户。

### 1.3 核心价值
- **统一视图**：打破平台壁垒，将不同格式的职位数据统一呈现。
- **降噪提效**：通过强大的过滤和企业黑名单系统，快速屏蔽外包或不匹配的企业。
- **智能诊断**：AI 简历与岗位深度对比，挖掘优劣势并生成破冰话术。
- **全栈追踪**：从“待投递”到“一面/二面/Offer”的 Kanban 拖拽式流程管理。

---

## 2. 架构与业务流 (Architecture & Business Flow)

### 2.1 数据流转机制
1. **数据导入**：通过外部爬虫/插件等手段抓取各平台的职位 JSON 原始数据（`rawData`），通过 API (`/api/sync-all`, `/api/jobs`) 导入系统。
2. **清洗与适配**：在数据呈现层前，后端使用**策略模式清洗器**（`jobNormalizer`）对不同平台的嵌套结构进行提取与标准化，转化为纯净的 `NormalizedJobData` 供前端使用。
3. **AI 分析与加工**：调用配置好的 LLM 接口，独立将当前岗位描述与个人简历比对，生成评分、诊断文本与招呼语。
4. **状态流转**：职位数据在待投递、已投递、面试、Offer 等状态间通过前端看板流转，数据落盘至本地 SQLite。

### 2.2 数据存储设计
采用本地 SQLite (Prisma ORM) 存储：
- **`Job`**: 核心实体，保留 `rawData` 供追溯，存储统一状态、标签、收藏等标识。
- **`AiJobResult`**: 弱关联结构，与 `Job` 解耦（方便在清空职位时保留诊断记录）。
- **`Company` / `BlacklistedCompany`**: 独立的企业库与黑名单库。
- **`Interview`**: 与 `Job` 强关联，记录面试轮次、时间、形式及复盘。
- **`AiSettings`**: 保存多套 AI Prompt Profile 与简历文本。
- **`QuestionBank`**: 本地面试题库，用于面试前的复习准备。

---

## 3. 核心功能需求 (Core Feature Requirements)

### 3.1 职位管理列表 (Jobs List)
- **多维度筛选**：支持按招聘平台（Boss/51job/智联/猎聘等）、职位状态（全部/未投递/已投递等）、学历要求进行筛选，支持关键字全文检索。
- **批量操作与动作面板**：
  - **收藏/取消收藏**（⭐）：标记意向职位。
  - **废弃/不合适**（👎/⛔）：快捷标记不合适的职位并附加原因（如外包、薪资低）。
  - **一键拉黑**：将发布该职位的公司直接加入黑名单库，隐藏该公司所有当前及未来导入的职位。
- **智能分页与加载**：处理海量（千级别）职位的快速渲染与分页。

### 3.2 职位详情抽屉与 AI 分析 (Job Detail & AI Diagnosis)
- **标准化详情展示**：右侧抽屉无缝展示规范化后的字段，如公司全称、融资阶段、规模、HR姓名/活跃度、薪水、职位描述以及各项福利技能标签。
- **AI 智能匹配分析**：
  - 用户触发“AI 分析”后，结合内置简历和职位要求，生成 `1-100` 匹配评分。
  - 展示匹配亮点与能力缺失点分析。
  - 自动生成符合该职位语境的“打招呼沟通文案”。
- **快捷跳转**：提供直达招聘平台原网址的锚点按钮。

### 3.3 投递追踪看板 (Kanban Tracker)
- **泳道设计**：包含「待投递」、「已投递」、「一面」、「二面」、「Offer」五个核心泳道。
- **拖拽流转**：通过 HTML5 Drag & Drop，实现卡片在不同泳道间的拖动，松手后自动通过 API 提交 `PUT /api/jobs/:id/status` 更新状态。
- **卡片精简信息**：看板卡片仅展示职位名、公司名、薪资与关键标签，保持界面清爽。

### 3.4 企业黑名单管理 (Company Blacklist)
- **黑名单列表**：独立页面管理已被拉黑的企业。
- **静默过滤**：后端在获取职位列表 (`/api/jobs`) 时，通过 `NOT IN` 或外键连接，自动把在 `BlacklistedCompany` 中的公司职位屏蔽，不在前端展示。

### 3.5 面试复盘与题库 (Interviews & Question Bank)
- **面试记录绑定**：在职位详情中可以添加多轮面试记录（现场/视频/电话），填写面经与复盘。
- **题库归档**：单独的 `/questions` 页面用于归档技术或行为面试题，支持标签化分类。

### 3.6 AI 设定与简历管理 (AI Settings)
- **简历编辑**：支持纯文本粘贴或 Markdown 格式简历保存，作为 AI 诊断的基准物料。
- **Profile 管理**：配置大语言模型（如 OpenAI、智谱等）的 API Key、模型版本与 System Prompt。

---

## 4. 业务逻辑规则 (Business Logic Rules)

### 4.1 数据清洗与字段适配逻辑
由于不同平台抓取的数据格式严重不一致，系统遵循以下清洗原则：
- **隔离原则**：数据库持久化存储原始抓取数据（`rawData` JSON 字符串），不做破坏性解析，确保原始数据随时可追溯。
- **渲染即清洗**：在调用 `/api/jobs` 给前端吐数据前，通过 `jobNormalizer` 根据 `platform` 字段分发到不同的适配器（如 `normalizeBossJob`）。
- **无痕传递**：从后端传给前端的数据会使用对象解构剔除巨大的 `rawData`，减少网络带宽消耗，仅传递 `NormalizedJobData`。

### 4.2 状态流转规则
- 职位状态枚举：`normal` (默认), `to-apply` (待投), `applied` (已投), `interview-1`, `interview-2`, `offer`, `rejected`。
- 如果职位未设定看板状态但被标记了“收藏 (isFavorited)”，在看板中默认归入 `to-apply` 泳道。

---

## 5. 用户交互与体验 (User Interaction & Experience)

### 5.1 交互规范 (UI/UX)
- **Element Plus 赋能**：统一使用 Element Plus 组件（如 `el-table`, `el-drawer`, `el-card`, `el-select`）确保界面风格统一、现代、且具有良好的交互反馈（如 Loading 态、Message 提示）。
- **即时响应**：收藏、拉黑、状态变更等操作采用“乐观更新” (Optimistic Update) 策略：先在界面上给出成功反馈并改变 UI 状态，后台静默请求；若接口报错，则回滚 UI 状态并弹窗提示错误。

### 5.2 抽屉联动交互
- 用户在列表页、看板页点击职位卡片或标题，均从屏幕右侧滑出全屏或定宽的 `JobDetailDrawer`。
- 抽屉内嵌滚动区域，信息分块卡片化展示（基本信息区、AI分析区、JD区、面试记录区），右下角或底部悬浮操作栏方便长篇阅读时快速操作。
