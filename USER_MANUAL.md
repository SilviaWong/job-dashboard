# 用户使用手册 (User Manual)

欢迎使用 **Job Dashboard Local (本地求职看板系统)**！
这是一款专为求职者打造的个人求职管理与追踪工具。它可以将您在各个招聘平台看到的职位统一收集起来，利用 AI 帮助您分析岗位匹配度，并通过现代化的拖拽看板帮助您追踪每一次投递。

---

## 1. 系统配置与启动

### 1.1 启动系统

在项目文件夹下打开终端，依次运行以下命令：

```bash
# 1. 首次使用前确保数据库准备完毕
npx prisma db push

# 2. 启动本地服务
npm run dev
```

启动成功后，在浏览器中访问 `http://localhost:3000` 即可打开系统主页。

---

## 2. 菜单导航与核心功能

系统左侧栏提供了 6 个核心功能菜单。以下将详细介绍各个页面的操作按钮与功能。

### 2.1 职位大厅 (Jobs)
求职信息的主战场，用于海量职位的筛选、操作与 AI 诊断。

*   **顶部过滤区**
    *   **搜索框**: 支持通过输入关键字模糊搜索职位名称或公司名称。
    *   **下拉筛选**: 包含**平台筛选**（全部、Boss直聘、前程无忧、智联招聘、猎聘）和**学历筛选**（大专、本科、硕士等）。
    *   **状态与快捷开关**: 支持切换常规状态，以及提供 `只看收藏`、`只看隐藏`、`显示拉黑` 的 Checkbox 快捷过滤。
    *   **刷新数据 按钮**: 手动重新加载职位列表。
*   **职位卡片操作栏 (卡片底部)**
    *   **⭐ 收藏 / 已收藏**: 将职位加入收藏，同时该职位会自动进入“投递追踪”看板的“待投递”列中。
    *   **👎 不合适**: 点击后弹出“标记为不合适”弹窗，可点选预设标签（如“学历不符”、“薪资太低”、“外包岗位”等）或直接回车输入自定义原因。点击“保存并隐藏”后，职位将在常规列表中消失。
    *   **⛔ 拉黑 / 已拉黑**: 将该职位的所属公司加入黑名单，即刻隐藏该公司下的所有岗位。
    *   **删除**: 彻底删除该条职位记录。
    *   **📅 面试**: 快捷打上面试标记状态。
    *   **🤖 AI 诊断**: 点击后，后台将结合您的简历与岗位要求进行 AI 智能分析，卡片右上角将渲染具体的匹配得分 (0-100分)。
    *   **详情 🔗**: 打开屏幕右侧的职位详情抽屉。
*   **职位详情抽屉 (Job Detail Drawer)**
    *   **左侧客观详情**: 展示基础数据、九宫格岗位概览、公司福利与原始排版的职位描述。底部提供 **查看原网页**（外链跳转）和 **⛔ 标记失效** 按钮。
    *   **右侧 AI 面板**: 包含直观的半环形匹配度评分、详细的优劣势长文分析。底部包含 **🤖 生成自我介绍** 按钮，一键让 AI 为您撰写可直接复制给 HR 的打招呼话术。

### 2.2 企业全景 (Companies)
以企业为维度的黄页与全景概览。

*   **顶部操作**
    *   **搜索框**: 根据公司名称进行搜索。
    *   **刷新数据 按钮**: 重新拉取企业库数据。
*   **列表展示 (手风琴折叠面板)**
    *   列表显示公司的行业、融资阶段、规模及收录的职位总数。
    *   **展开公司面板后**，可详细看到“公司介绍”、“公司福利”。
    *   下方以网格 (Grid) 形式罗列该公司旗下所有的**职位小卡片**。
    *   **小卡片点击**: 同样会呼出“职位详情抽屉”。若职位已失效或被标记为不合适，卡片将呈灰色半透明显示。

### 2.3 投递追踪 (Kanban)
可视化的求职进度管理器。

*   **顶部操作**
    *   **刷新数据 按钮**: 同步看板最新状态。
*   **泳道列与拖拽 (Drag & Drop)**
    *   看板横向划分为 5 个泳道：`待投递` -> `已投递` -> `一面` -> `二面` -> `Offer`。
    *   **拖拽卡片**: 使用鼠标按住某张卡片（指针会变成抓取状态），直接将其拖动到目标泳道内松手。系统将乐观响应并自动把新状态保存到数据库中。

### 2.4 大模型管理 (AI Model)
配置多个平台的 LLM (大语言模型) API 密钥与地址。

*   **左侧边栏**
    *   展示所有已添加的模型配置。
    *   顶部右侧有 **➕ 新增配置** 按钮 (加号图标)。
*   **右侧表单面板**
    *   **表单功能**: 填写配置名称（如: 阿里通义千问）、API Base URL、模型名称、API Key。
    *   **删除此配置 按钮**: 仅当存在多个配置时出现，点击确认后删除当前高亮的配置。
    *   **测试连接 按钮**: 快速检测填写的 URL 和 Key 是否能正确连通 AI 大模型。
    *   **保存配置 按钮**: 提交并保存表单。

### 2.5 简历管理 (AI Resume)
用于提供个人背景信息，这是 AI 智能诊断及生成打招呼话术的核心依据。

*   **简历编辑器**: 占据页面的超大文本域，支持输入或粘贴纯文本及 Markdown 格式的简历内容。
*   **保存简历 按钮**: 位于页面右下角，将简历内容持久化到系统配置中。

### 2.6 题库管理 (Questions)
日常刷题及面试复盘知识库。

*   **顶部操作区**
    *   **搜索框**: 支持根据题干或标签进行搜索过滤。
    *   **📥 导入 Excel 按钮**: 未来支持批量导入外部题库。
    *   **➕ 新增题目 按钮**: 呼出创建题目表单。
    *   **🗑️ 清空题库 按钮**: 极度危险操作，点击后进行二次确认，清空所有题库数据。
*   **列表功能**
    *   显示序号、题干、分类、与高亮标签。
    *   操作列提供 **编辑** 与 **删除** 按钮。
    *   页面底部包含可切换页码与页面大小的 **分页器**。
*   **新增/编辑题目弹窗**
    *   表单项：序号、题干长文本、主题分类、二级分类、支持下拉多选和回车创建的标签框。
    *   答案编辑区上方包含 **🤖 AI 帮我答 (结合简历)** 按钮，可直接调用 AI 生成针对性解答。
    *   右下角包含 **🗑️ 删除该题**（编辑模式下）、**取消**、**保存** 按钮。

---

## 3. 常见问题 (FAQ)

**Q: 为什么我刚导入的职位在列表里看不到？**
> A: 请检查您是否开启了顶部工具栏中的某些过滤开关（如选中了“只看隐藏”），或者该职位所属的公司已被您加入过黑名单（可勾选“显示拉黑”进行排查）。

**Q: AI 诊断总是失败，是什么原因？**
> A: 请前往左侧菜单的 **[大模型管理]** 页面，确保您的 API Base URL、模型名称、以及 API Key 配置正确无误，您可以点击“测试连接”按钮验证其有效性。

**Q: 职位被我误操作标为“不合适 (隐藏)”，怎么恢复？**
> A: 在【职位大厅】顶部工具栏勾选“只看隐藏”复选框，找到被隐藏的职位后，若希望取消隐藏，目前可通过修改标签或重新标定状态来恢复，或者彻底删除重新抓取。

**Q: 系统数据会上传到云端泄露我的隐私吗？**
> A: **绝对不会**。Job Dashboard Local 所有的职位数据、您的简历文本以及面经复盘，全部加密存储在您本地电脑上的数据库文件中，没有任何上传云端的代码，请放心使用。
